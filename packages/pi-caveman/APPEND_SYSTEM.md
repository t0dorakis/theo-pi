# APPEND_SYSTEM.md

> **Note:** This file is NOT a pi-recognized convention and is not read automatically by pi.
> It exists as documentation only. The actual always-on system prompt injection is done by
> `extensions/caveman.ts` via the `before_agent_start` event hook.
>
> If you want this skill active by default in a project, add the following to your AGENTS.md:
>
> ```
> Always activate installed skill `pi-caveman` in full mode by default.
> ```
